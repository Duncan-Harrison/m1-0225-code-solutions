/* exported getSumOfSquares */
function getSumOfSquares(x: number, y: number): number {
  const firstSquare: number = x ** 2;
  const secondSquare: number = y ** 2;
  return firstSquare + secondSquare;
}
